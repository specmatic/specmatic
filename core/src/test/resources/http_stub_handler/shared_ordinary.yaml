openapi: 3.0.3
info:
  title: Ordinary API
  version: 1.0.0
paths:
  /shared:
    get:
      responses:
        '200':
          description: Shared text
          content:
            text/plain:
              schema:
                type: string
