openapi: 3.0.0
info: { title: api, version: "1" }
paths:
  /p:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "./ProductBase.yaml"
      responses: { "200": { description: ok } }
