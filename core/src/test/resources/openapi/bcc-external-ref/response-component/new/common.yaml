openapi: 3.0.0
info: { title: common, version: "1" }
paths: {}
components:
  responses:
    OK:
      description: ok
      content:
        application/json:
          schema:
            type: object
            required: [name]
            properties:
              name: { type: integer }