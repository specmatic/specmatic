openapi: 3.0.0
info: { title: common, version: "1" }
paths: {}
components:
  schemas:
    Node:
      type: object
      required: [value]
      properties:
        value: { type: string }
        next:
          $ref: "#"
