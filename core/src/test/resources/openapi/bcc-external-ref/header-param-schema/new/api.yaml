openapi: 3.0.0
info: { title: api, version: "1" }
paths:
  /p:
    get:
      parameters:
        - name: X-Tenant
          in: header
          required: true
          schema:
            $ref: "./common.yaml#/components/schemas/Scalar"
      responses: { "200": { description: ok } }
