openapi: 3.0.0
info: { title: common, version: "1" }
paths: {}
components:
  schemas:
    Req:
      type: object
      required: [id, token]
      properties:
        id: { type: string }
        token: { type: string }
