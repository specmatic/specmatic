openapi: 3.0.0
info:
  title: Orders API
  version: 1.0.0
paths:
  /orders:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Order'
        '400':
          description: bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorBrief'
        # OLD-SIDE CHANGE O3 (breaking): old declares a 500 response on GET /orders;
        # new removes it entirely.
        # Expected: GET /orders 500 row CHANGED + Incompatible
        # (BCC iterates old scenarios, finds no matching status in new, emits
        # "exists in old but not in new").
        '500':
          description: server error
          content:
            application/json:
              schema:
                type: object
                required: [message]
                properties:
                  message:
                    type: string
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrderInput'
      responses:
        '201':
          description: created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
        # UNCHANGED 5-tuple under a CHANGED (path, method): POST /orders 400
        # uses OrderInput (untouched) for request and ValidationError (untouched)
        # for response. Sits alongside POST /orders 201 (CHANGED because Order
        # in the response gained `notes`). The clearest per-5-tuple demonstration.
        '400':
          description: bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
  /orders/{id}:
    get:
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
        '404':
          description: not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorDetailed'
    # OLD-SIDE CHANGE O2 (breaking): old declares DELETE /orders/{id}; new removes
    # the operation entirely.
    # Expected: DELETE /orders/{id} 204 row CHANGED + Incompatible
    # (operation removal is the canonical breaking change).
    delete:
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: deleted
  # UNCHANGED operation: identical in both specs, returns UNCHANGED + Compatible.
  /health:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
  /categories:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Category'
  # allOf case: Shipment composes a $ref'd constituent (ShipmentBase) and an
  # inline constituent. Both contribute a property that turns breaking in new.
  /shipments:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Shipment'
  # WIP operation: tagged WIP so it is ignored for the pass/fail verdict, but it
  # must still execute and be change-tracked. Its `code` response field changes
  # string -> integer in new (breaking).
  # Expected: GET /promotions 200 row CHANGED + Incompatible + qualifier `wip`,
  # counted under `other` (not `failed`) in the summary, and not breaking the verdict.
  /promotions:
    get:
      tags: [WIP]
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                required: [code]
                properties:
                  code:
                    type: string
  # reffed path-param case: widgetId is $ref'd to a component parameter whose
  # schema is string here and integer in new (breaking). The breadcrumb must
  # resolve to the component parameter's `name` key even though the $ref
  # use-site has no `name`.
  /widgets/{widgetId}:
    put:
      parameters:
        - $ref: '#/components/parameters/WidgetId'
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                required: [id]
                properties:
                  id:
                    type: string
components:
  schemas:
    Order:
      type: object
      required: [id, total, customer]
      properties:
        id:
          type: string
        total:
          type: number
        customer:
          $ref: '#/components/schemas/Customer'
    # UNCHANGED component: dedicated request-side schema kept separate from
    # `Order` so that NEW-SIDE CHANGE N1 (adding `notes` to Order) doesn't
    # propagate into POST /orders' request fingerprint. This is what lets
    # POST /orders 400 stay UNCHANGED while POST /orders 201 turns CHANGED.
    OrderInput:
      type: object
      required: [id, total, customer]
      properties:
        id:
          type: string
        total:
          type: number
        customer:
          $ref: '#/components/schemas/Customer'
    Customer:
      type: object
      required: [name, email]
      properties:
        name:
          type: string
        email:
          type: string
    ErrorBrief:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
    # UNCHANGED component: referenced only by POST /orders 400.
    ValidationError:
      type: object
      required: [field, reason]
      properties:
        field:
          type: string
        reason:
          type: string
    ErrorDetailed:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
        # OLD-SIDE CHANGE O1 (non-breaking): old declares optional `traceId` on
        # ErrorDetailed; new removes the field.
        # Expected: GET /orders/{id} 404 row CHANGED + Compatible
        # (dropping an optional response field is non-breaking - old clients
        # never required it to be present).
        traceId:
          type: string
    Category:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
        name:
          type: string
        children:
          type: array
          items:
            $ref: '#/components/schemas/Category'
    # allOf source-location case: `weight` from the $ref'd constituent
    # (ShipmentBase) and `carrier` from the inline constituent, both string
    # here and number in new (breaking).
    ShipmentBase:
      type: object
      required: [weight]
      properties:
        weight:
          type: string
    Shipment:
      allOf:
        - $ref: '#/components/schemas/ShipmentBase'
        - type: object
          required: [carrier]
          properties:
            carrier:
              type: string
  parameters:
    WidgetId:
      name: widgetId
      in: path
      required: true
      schema:
        type: string
