openapi: 3.0.3
x-specmatic-spec-ref: 'OAS 3.0.3 Response Object — response with no body (empty content): https://spec.openapis.org/oas/v3.0.3.html#response-object'
info:
  title: "Request/Response body - empty response body (204)"
  version: "1.0"
paths:
  /notifications:
    post:
      operationId: markNotificationsRead
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ids
              properties:
                ids:
                  type: array
                  items:
                    type: integer
      responses:
        '204':
          description: No content
