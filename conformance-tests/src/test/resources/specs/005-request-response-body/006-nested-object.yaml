openapi: 3.0.3
x-specmatic-spec-ref: 'OAS 3.0.3 Schema Object — nested object property: https://spec.openapis.org/oas/v3.0.3.html#schema-object'
info:
  title: "Request/Response body - nested object"
  version: "1.0"
paths:
  /contacts:
    get:
      operationId: listContacts
      responses:
        '200':
          description: Contact list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - name
                    - address
                  properties:
                    name:
                      type: string
                    address:
                      type: object
                      required:
                        - street
                        - city
                        - zip
                      properties:
                        street:
                          type: string
                        city:
                          type: string
                        zip:
                          type: string
    post:
      operationId: createContact
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - address
              properties:
                name:
                  type: string
                address:
                  type: object
                  required:
                    - street
                    - city
                    - zip
                  properties:
                    street:
                      type: string
                    city:
                      type: string
                    zip:
                      type: string
      responses:
        '201':
          description: Created
