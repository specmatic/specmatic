openapi: 3.0.3
x-specmatic-spec-ref: 'OAS 3.0.3 Media Type Object — text/plain response (RFC 9110 §8.3.1): https://spec.openapis.org/oas/v3.0.3.html#media-type-object'
info:
  title: "Content negotiation - plain text response"
  version: "1.0"
paths:
  /health:
    get:
      operationId: healthCheck
      summary: Health check returning plain text
      responses:
        '200':
          description: Health status
          content:
            text/plain:
              schema:
                type: string
