openapi: 3.0.3
info:
  title: "Schema - integer property"
  version: "1.0"
paths:
  /counter:
    get:
      operationId: getCounter
      responses:
        '200':
          description: Counter
          content:
            application/json:
              schema:
                type: object
                required:
                  - value
                properties:
                  value:
                    type: integer
