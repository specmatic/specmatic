openapi: 3.0.3
info:
  title: "Schema - boolean property"
  version: "1.0"
paths:
  /feature-flag:
    get:
      operationId: getFlag
      responses:
        '200':
          description: Feature flag
          content:
            application/json:
              schema:
                type: object
                required:
                  - enabled
                properties:
                  enabled:
                    type: boolean
