openapi: 3.0.3
info:
  title: "Response header - optional"
  version: "1.0"
paths:
  /items:
    get:
      operationId: listItems
      responses:
        '200':
          description: Items list
          headers:
            X-Request-Id:
              required: true
              schema:
                type: string
            X-Deprecation-Notice:
              required: false
              schema:
                type: string
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - id
                  properties:
                    id:
                      type: integer
