openapi: 3.0.3
info:
  title: "String with regex pattern"
  version: "1.0"
paths:
  /codes:
    get:
      operationId: listCodes
      responses:
        '200':
          description: Code list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - label
                    - code
                  properties:
                    label:
                      type: string
                    code:
                      type: string
                      pattern: "^[A-Z]{3}-[0-9]{4}$"
    post:
      operationId: createCode
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - label
                - code
              properties:
                label:
                  type: string
                code:
                  type: string
                  pattern: "^[A-Z]{3}-[0-9]{4}$"
      responses:
        '201':
          description: Created
