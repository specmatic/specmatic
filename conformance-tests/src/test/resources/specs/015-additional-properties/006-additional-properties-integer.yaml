openapi: 3.0.3
x-specmatic-spec-ref: 'JSON Schema — `additionalProperties` of integer type: https://json-schema.org/draft/2020-12/json-schema-core#name-additionalproperties'
info:
  title: "additionalProperties with integer values"
  version: "1.0"
paths:
  /scores:
    get:
      operationId: getScores
      responses:
        '200':
          description: Score map
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: integer
    post:
      operationId: createScores
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties:
                type: integer
      responses:
        '201':
          description: Created
