openapi: 3.1.0
x-specmatic-spec-ref: 'OAS 3.1 — `nullable` is removed; ignored if present (use `type` array instead): https://spec.openapis.org/oas/v3.1.1.html#schema-object'
info:
  title: "OpenAPI 3.1 - nullable keyword should be ignored"
  version: "1.0"
paths:
  /contacts:
    get:
      operationId: listContacts
      responses:
        '200':
          description: Contact list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Contact'
    post:
      operationId: createContact
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Contact'
      responses:
        '201':
          description: Created
components:
  schemas:
    Contact:
      type: object
      required:
        - id
        - name
        - phone
      properties:
        id:
          type: integer
        name:
          type: string
        phone:
          type: string
          nullable: true
