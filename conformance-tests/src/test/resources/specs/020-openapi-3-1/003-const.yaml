openapi: 3.1.0
x-specmatic-spec-ref: 'JSON Schema 2020-12 — `const` keyword: https://json-schema.org/draft/2020-12/json-schema-validation#name-const'
info:
  title: "OpenAPI 3.1 - const keyword"
  version: "1.0"
paths:
  /events:
    get:
      operationId: listEvents
      responses:
        '200':
          description: Event list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - id
                    - type
                    - source
                  properties:
                    id:
                      type: integer
                    type:
                      const: "notification"
                    source:
                      type: string
    post:
      operationId: createEvent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - id
                - type
                - source
              properties:
                id:
                  type: integer
                type:
                  const: "notification"
                source:
                  type: string
      responses:
        '201':
          description: Created
