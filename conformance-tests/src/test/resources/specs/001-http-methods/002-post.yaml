openapi: 3.0.3
x-specmatic-spec-ref: 'OAS 3.0.3 Operation Object — POST method semantics (RFC 9110 §9.3.3): https://spec.openapis.org/oas/v3.0.3.html#operation-object'
info:
  title: "POST with request body"
  version: "1.0"
paths:
  /items:
    post:
      operationId: createItem
      summary: Create an item
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
      responses:
        '201':
          description: Item created
          content:
            application/json:
              schema:
                type: object
                required:
                  - id
                properties:
                  id:
                    type: integer
