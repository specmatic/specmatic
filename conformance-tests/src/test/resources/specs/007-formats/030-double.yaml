openapi: 3.0.3
x-specmatic-spec-ref: 'OAS 3.0.3 number format — `double` (IEEE 754 double): https://spec.openapis.org/oas/v3.0.3.html#data-types'
info:
  title: "Format - double (OpenAPI)"
  version: "1.0"
paths:
  /coordinates:
    get:
      operationId: getCoordinates
      responses:
        '200':
          description: Coordinates
          content:
            application/json:
              schema:
                type: object
                required:
                  - latitude
                  - longitude
                properties:
                  latitude:
                    type: number
                    format: double
                  longitude:
                    type: number
                    format: double
    post:
      operationId: createCoordinates
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - latitude
                - longitude
              properties:
                latitude:
                  type: number
                  format: double
                longitude:
                  type: number
                  format: double
      responses:
        '201':
          description: Created
