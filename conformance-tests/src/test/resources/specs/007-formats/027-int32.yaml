openapi: 3.0.3
x-specmatic-spec-ref: 'OAS 3.0.3 integer format — `int32` (signed 32-bit): https://spec.openapis.org/oas/v3.0.3.html#data-types'
info:
  title: "Format - int32 (OpenAPI)"
  version: "1.0"
paths:
  /metrics:
    get:
      operationId: getMetrics
      responses:
        '200':
          description: Metrics
          content:
            application/json:
              schema:
                type: object
                required:
                  - requestCount
                  - errorCount
                properties:
                  requestCount:
                    type: integer
                    format: int32
                  errorCount:
                    type: integer
                    format: int32
    post:
      operationId: createMetrics
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - requestCount
                - errorCount
              properties:
                requestCount:
                  type: integer
                  format: int32
                errorCount:
                  type: integer
                  format: int32
      responses:
        '201':
          description: Created
