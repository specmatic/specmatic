openapi: 3.0.3
x-specmatic-spec-ref: 'JSON Schema "email" format (RFC 5321): https://json-schema.org/draft/2020-12/json-schema-validation#name-email-addresses'
info:
  title: "Format - email (JSON Schema)"
  version: "1.0"
paths:
  /contacts:
    get:
      operationId: listContacts
      responses:
        '200':
          description: Contact list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - name
                    - email
                  properties:
                    name:
                      type: string
                    email:
                      type: string
                      format: email
    post:
      operationId: createContacts
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - email
              properties:
                name:
                  type: string
                email:
                  type: string
                  format: email
      responses:
        '201':
          description: Created
