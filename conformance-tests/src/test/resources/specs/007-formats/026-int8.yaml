openapi: 3.0.3
x-specmatic-spec-ref: 'OAS 3.x integer format extension — `int8` (8-bit signed): https://spec.openapis.org/oas/v3.0.3.html#data-types'
info:
  title: "Format - int8 (OpenAPI)"
  version: "1.0"
paths:
  /flags:
    get:
      operationId: getFlags
      responses:
        '200':
          description: Flags
          content:
            application/json:
              schema:
                type: object
                required:
                  - level
                  - priority
                properties:
                  level:
                    type: integer
                    format: int8
                  priority:
                    type: integer
                    format: int8
    post:
      operationId: createFlags
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - level
                - priority
              properties:
                level:
                  type: integer
                  format: int8
                priority:
                  type: integer
                  format: int8
      responses:
        '201':
          description: Created
