openapi: 3.0.3
x-specmatic-spec-ref: 'OAS 3.0.3 "byte" format — base64-encoded characters (RFC 4648): https://spec.openapis.org/oas/v3.0.3.html#data-types'
info:
  title: "Format - byte (OpenAPI, base64-encoded)"
  version: "1.0"
paths:
  /thumbnails:
    get:
      operationId: listThumbnails
      responses:
        '200':
          description: Thumbnail list
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - name
                    - data
                  properties:
                    name:
                      type: string
                    data:
                      type: string
                      format: byte
    post:
      operationId: createThumbnails
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - data
              properties:
                name:
                  type: string
                data:
                  type: string
                  format: byte
      responses:
        '201':
          description: Created
